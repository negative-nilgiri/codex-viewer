# Viewer frontend

Minimal React and TypeScript client for the Codex Sessions Viewer API.

```bash
npm ci
npm run dev
npm run lint
npm run build
```

The production build is copied into the Caddy image by `server/Dockerfile`.
